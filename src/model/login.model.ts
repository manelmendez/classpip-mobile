/**
 * Data model used for view-controller binding. This object is
 * used to send the data to the server via a POST request
 */
export class Login {
  public username: string;
  public password: string;
}
